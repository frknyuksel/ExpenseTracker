/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['assets.aceternity.com'], // Güvenli kaynaklar listesine ekleyin
  },
};


export default nextConfig;
